// Hardcoded access gate — there's no real admin auth system yet, just a
// numeric code shared across the admin-only pages (photo review, guest
// list) that keeps them from being stumbled onto.
export const ADMIN_ACCESS_CODE = '92726'
